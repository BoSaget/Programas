// Funciones = Function  Expresion

//Las function expression solo se deben de declaraar antes d eusarlar por ser declaradas como una variable
const sumar = function(numero1, numero2) {
    return numero1+numero2
}

const resultado = sumar(30, 20)
console.log(resultado)